---
name: handoff
description: Compact conversation into handoff doc for another agent. Use when handing off session work.
argument-hint: "What will the next session be used for?"
---

Write handoff doc summarizing conversation so fresh agent continues. Save to OS temp dir — not workspace.

Include **suggested skills** section (skills next agent should invoke).

Do not duplicate content in other artifacts (PRDs, plans, ADRs, issues, commits, diffs). Reference by path/URL.

Redact sensitive info (API keys, passwords, PII).

If user passed arguments → treat as next-session focus; tailor doc.
